# Vercel installs these for the dashboard function — NOT the root
# requirements.txt, which pulls sentence-transformers and therefore torch
# (~800MB, well past the 250MB function limit). The engine runs on Manan's Mac
# and needs those; the viewer does not.
fastapi>=0.115
sqlalchemy>=2.0
psycopg[binary]>=3.2
pydantic>=2
pydantic-settings>=2
jinja2>=3.1
httpx>=0.27
google-genai>=1.0
langgraph>=0.2
feedparser>=6.0
